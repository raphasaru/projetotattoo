---
const calendlyUrl = "https://calendly.com/projetotattoo/visita";
---

<section id="agendar" class="final-cta-section">
  <div class="cta-background">
    <div class="ink-splash-1"></div>
    <div class="ink-splash-2"></div>
    <div class="grid-overlay"></div>
  </div>

  <div class="container">
    <div class="cta-content">
      <span class="urgency-badge">
        <span class="pulse-dot"></span>
        Visitas disponíveis — agenda limitada
      </span>

      <h2 class="cta-headline">
        Venha conhecer<br/>
        <span class="highlight">a escola pessoalmente</span>
      </h2>

      <p class="cta-subtext">
        Conheça nosso espaço em <strong>Santana</strong>, veja os equipamentos, tire todas as dúvidas e sinta o ambiente antes de decidir.<br/>
        Sem compromisso. Só você e a nossa equipe.
      </p>

      <div class="cta-features">
        <div class="feature">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <polyline points="20 6 9 17 4 12"/>
          </svg>
          <span>Tour pela escola</span>
        </div>
        <div class="feature">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <polyline points="20 6 9 17 4 12"/>
          </svg>
          <span>Conversa com o professor</span>
        </div>
        <div class="feature">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <polyline points="20 6 9 17 4 12"/>
          </svg>
          <span>Sem compromisso</span>
        </div>
      </div>

      <div class="address-card">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" class="pin-icon">
          <path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7z"/>
          <circle cx="12" cy="9" r="2.5"/>
        </svg>
        <div>
          <span class="address-label">Unidade Santana</span>
          <span class="address-text">R. Alferes Magalhães, 92 — Sala 137</span>
        </div>
      </div>

      <!-- Calendly inline embed -->
      <div class="calendly-wrapper">
        <div
          class="calendly-inline-widget"
          data-url={calendlyUrl}
          style="min-width:320px;height:1000px;"
        ></div>
        <script type="text/javascript" src="https://assets.calendly.com/assets/external/widget.js" async></script>
      </div>
    </div>
  </div>
</section>

<style>
  .final-cta-section {
    position: relative;
    padding: clamp(5rem, 12vw, 10rem) 0 clamp(3rem, 8vw, 6rem);
    overflow: hidden;
  }

  .cta-background {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, var(--black) 0%, #0d1a0d 50%, var(--black) 100%);
  }

  .ink-splash-1 {
    position: absolute;
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(34, 197, 94, 0.15) 0%, transparent 60%);
    top: -200px;
    right: -200px;
    filter: blur(80px);
    animation: float 10s ease-in-out infinite;
  }

  .ink-splash-2 {
    position: absolute;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(201, 169, 98, 0.1) 0%, transparent 60%);
    bottom: -150px;
    left: -150px;
    filter: blur(80px);
    animation: float 8s ease-in-out infinite reverse;
  }

  @keyframes float {
    0%, 100% { transform: translate(0, 0); }
    50% { transform: translate(20px, -20px); }
  }

  .grid-overlay {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(34, 197, 94, 0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(34, 197, 94, 0.03) 1px, transparent 1px);
    background-size: 60px 60px;
  }

  .cta-content {
    position: relative;
    z-index: 1;
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }

  .urgency-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1.5rem;
    background: rgba(201, 169, 98, 0.1);
    border: 1px solid rgba(201, 169, 98, 0.3);
    border-radius: 100px;
    color: var(--gold);
    font-size: 0.85rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 2rem;
  }

  .pulse-dot {
    width: 10px;
    height: 10px;
    background: var(--gold);
    border-radius: 50%;
    animation: blink 1s infinite;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.3; }
  }

  .cta-headline {
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 6vw, 4rem);
    line-height: 1.1;
    text-transform: uppercase;
    margin-bottom: 1.5rem;
  }

  .highlight {
    color: var(--green);
    display: block;
  }

  .cta-subtext {
    font-size: 1.1rem;
    color: var(--white-dim);
    line-height: 1.7;
    margin-bottom: 2rem;
  }

  .cta-subtext strong {
    color: var(--white);
  }

  .cta-features {
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin-bottom: 2rem;
    flex-wrap: wrap;
  }

  .feature {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--white-dim);
    font-size: 0.9rem;
  }

  .feature svg {
    width: 20px;
    height: 20px;
    color: var(--green);
  }

  .address-card {
    display: inline-flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem 1.5rem;
    background: rgba(201, 169, 98, 0.08);
    border: 1px solid rgba(201, 169, 98, 0.2);
    border-radius: 12px;
    margin-bottom: 3rem;
    text-align: left;
  }

  .pin-icon {
    width: 28px;
    height: 28px;
    color: var(--gold);
    flex-shrink: 0;
  }

  .address-label {
    display: block;
    font-size: 0.75rem;
    color: var(--gold);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-weight: 600;
    margin-bottom: 0.25rem;
  }

  .address-text {
    display: block;
    font-size: 0.95rem;
    color: var(--white-dim);
  }

  .calendly-wrapper {
    border-radius: 16px;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.08);
    background: #fff;
  }

  @media (max-width: 768px) {
    .cta-features {
      gap: 1rem;
    }

    .feature {
      font-size: 0.8rem;
    }

    .address-card {
      text-align: left;
    }
  }

  @media (max-width: 480px) {
    .cta-features {
      flex-direction: column;
      gap: 0.75rem;
    }

    .address-card {
      flex-direction: column;
      text-align: center;
    }
  }
</style>
